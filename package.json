{
  "name": "hublot",
  "version": "0.1.0",
  "description": "Broker + CLI pour piloter un unique Chromium visible et partage entre plusieurs agents, chacun scope a son propre onglet.",
  "private": true,
  "license": "LicenseRef-BZ-1.1",
  "bin": {
    "hublot": "./dist/cli/index.js"
  },
  "scripts": {
    "build": "tsc -p .",
    "dev": "tsc -p . --watch",
    "package": "npm run build && node scripts/package-sea.mjs"
  },
  "dependencies": {
    "commander": "^12.1.0",
    "playwright-core": "^1.47.0"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "esbuild": "^0.28.2",
    "postject": "^1.0.0-alpha.6",
    "typescript": "^5.5.0"
  },
  "engines": {
    "node": ">=18"
  }
}
